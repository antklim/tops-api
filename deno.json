{
  "fmt": {
    "exclude": [
      ".cache",
      ".github",
      ".vscode",
      ".gitignore",
      "deno.json",
      "lock.json",
      "README.md"
    ]
  },
  "lint": {
    "exclude": [
      ".cache"
    ]
  },
  "tasks": {
    "start": "deno run --allow-env=PORT --allow-net main.ts",
    "test": "deno test -A --unstable"
  }
}